# 사용 매뉴얼

이 문서는 API 서버가 이미 실행 중인 상태를 기준으로 작성했습니다.

## 1. 구글시트 운영 방법

1. 대회 운영용 구글시트는 살곶이 구글 계정의 스프레드시트에 있습니다.
2. 참가자는 `rank` 탭에 등록합니다.
3. 참가자 등록 시 `작대`, `이름`, `그룹`, `덤`을 입력합니다.
4. `그룹`에 `-`를 입력하면 좌불로 처리됩니다.
5. 각 순의 각 시에 관중이면 해당 칸에 `1`을 입력합니다.
6. 한 순을 쐈지만 0중이면 그 순의 시수 칸 중 하나 이상에 `0`을 입력합니다.
7. 한 순을 아직 쏘지 않았으면 해당 순의 5개 시수 칸을 모두 비워둡니다.
8. `합시`, `총합`, `1순 합`, `2순 합`, `3순 합`은 시트 수식으로 계산되도록 유지합니다.
9. 참가자 이름이 같은 행이 2개 이상 있으면 운영자 페이지에 중복으로 표시됩니다.
10. `순위` 컬럼은 없어도 됩니다. 순위는 서버에서 자동으로 계산합니다.
11. 컬럼 이름은 임의로 바꾸지 않습니다.

예시:
- 등록: `작대`, `이름`, `그룹`, `덤` 입력
- 관중 기록: 해당 시수 칸에 `1` 입력
- 0중 기록: 해당 순의 시수 칸 중 하나 이상에 `0` 입력
- 미시도 기록: 해당 순의 5개 시수 칸 전체를 빈칸으로 유지

## 2. 접속 주소

실제 접속 주소는 운영자가 별도로 전달합니다.

1. 브라우저를 엽니다.
2. 주소창에 운영자가 안내한 대회 페이지 주소를 입력합니다.

## 3. 일반 참가자 화면 사용법

일반 참가자는 운영자 페이지에 접속할 필요 없이 안내받은 대회 페이지에서 조회 화면만 사용합니다.

### 3-1. 메인 화면

1. `현재 등수 보기`를 누르면 개인 순위를 볼 수 있습니다.
2. `공지 / 상황판`을 누르면 공지 내용을 볼 수 있습니다.
3. `작대 구성`을 누르면 작대별 참가자 목록을 볼 수 있습니다.
4. `단체팀 구성`을 누르면 생성된 단체전 팀을 볼 수 있습니다.

### 3-2. 현재 등수 보기

1. 전체 순위를 확인합니다.
2. 상단의 그룹 버튼을 누르면 해당 그룹만 따로 볼 수 있습니다.
3. 순위는 서버가 자동 계산합니다.
4. `-` 표시는 해당 순을 안 쏜 상태입니다.
5. `0` 표시는 쐈지만 0중인 상태입니다.

### 3-3. 작대 구성 보기

1. 작대 번호별 참가자 목록을 확인합니다.
2. 참가자 이름 옆에서 그룹을 확인할 수 있습니다.
3. 그룹이 `-`인 참가자는 좌불로 표시됩니다.

### 3-4. 단체팀 보기

1. 생성된 팀 목록을 확인합니다.
2. 각 팀의 총점과 팀 간 점수 차이를 확인합니다.
3. 아직 팀이 생성되지 않았다면 운영자 페이지에서 팀을 생성해야 합니다.

### 3-5. 공지 화면 보기

1. 공지 화면에서 대회 공지나 상황판 내용을 확인합니다.
2. 공지가 보이지 않으면 아직 등록된 공지가 없거나 운영자가 공지 기능을 사용하지 않는 상태입니다.

## 4. 운영자 화면 사용법

운영자는 운영자 페이지에서 데이터 갱신, 팀 생성, 팀 초기화, 구글시트 주소 변경을 처리합니다.

### 4-1. 운영자 페이지 접속

1. 운영자가 별도로 전달한 운영자 페이지 주소로 접속합니다.
2. 운영자 페이지에서는 데이터 갱신, 팀 생성, 팀 초기화, 구글시트 주소 변경을 할 수 있습니다.

### 4-2. 데이터 갱신하기

1. 운영자 페이지에서 `데이터 갱신` 버튼을 누릅니다.
2. 구글시트의 최신 내용을 다시 불러옵니다.
3. 갱신 결과 화면에서 변경된 참가자, 공지, 작대 내용을 확인합니다.
4. `홈으로`를 누르면 운영자 페이지로 돌아갑니다.

### 4-3. 팀 생성하기

1. 운영자 페이지의 `팀 배정` 영역으로 이동합니다.
2. 만들 팀 수를 입력합니다.
3. `팀 생성` 버튼을 누릅니다.
4. 생성된 팀 화면에서 팀별 참가자와 점수 차이를 확인합니다.
5. `홈으로`를 누르면 운영자 페이지로 돌아갑니다.

### 4-4. 팀 초기화하기

1. 운영자 페이지에서 `팀 초기화` 버튼을 누릅니다.
2. 기존에 생성된 팀 배정 결과가 삭제됩니다.
3. 필요하면 다시 팀 수를 입력해 새 팀을 생성합니다.

### 4-5. 구글시트 주소 변경하기

1. 운영자 페이지의 `구글 시트` 영역으로 이동합니다.
2. 공유 주소 입력칸에 새 구글시트 주소를 붙여넣습니다.
3. `저장` 버튼을 누릅니다.
4. 정상 주소라면 저장 후 데이터가 자동 갱신됩니다.
5. 잘못된 주소이거나 권한 문제가 있으면 오류 메시지가 표시되고 기존 주소는 유지됩니다.

### 4-6. 중복 참가자 확인하기

1. 운영자 페이지의 요약 영역에서 `중복` 항목을 확인합니다.
2. 중복 참가자가 있으면 화면 상단에 이름과 중복 횟수가 표시됩니다.
3. 중복이 표시되면 구글시트에서 참가자 이름을 확인하고 수정합니다.
4. 수정 후 `데이터 갱신` 버튼을 누릅니다.

### 4-7. 좌불 처리하기

1. 구글시트의 `그룹` 값을 `-`로 입력하면 좌불로 처리됩니다.
2. 좌불 참가자는 순위와 단체전 팀 배정에서 제외됩니다.
3. 작대 구성 화면에서는 좌불 표시로 확인할 수 있습니다.

### 4-8. 공지 화면 관리하기

1. 구글시트에 `board` 탭이 있으면 공지 화면에 내용이 표시됩니다.
2. `board` 탭이 없어도 다른 기능은 정상 동작합니다.
3. 공지를 수정한 뒤 반영이 느리면 운영자 페이지에서 `데이터 갱신`을 누릅니다.

## 5. 자동 갱신

1. 서버는 10초마다 구글시트 데이터를 자동으로 다시 읽습니다.
2. 화면 자체가 자동으로 새로고침되지는 않습니다.
3. 바로 확인해야 할 때는 운영자 페이지에서 `데이터 갱신`을 누르거나 브라우저를 새로고침합니다.

## 6. 문제가 있을 때

1. 운영자 페이지 상단의 오류 메시지를 확인합니다.
2. 구글시트 주소와 공유 권한을 확인합니다.
3. `rank` 탭 이름이 정확한지 확인합니다.
4. 필수 컬럼 이름이 바뀌지 않았는지 확인합니다.
5. 확인 후 운영자 페이지에서 `데이터 갱신`을 누릅니다.

# 리팩터링 계획

## 목표

현재 FastAPI 서버를 기준으로 Google Sheets 데이터를 안정적으로 읽고, 대회 운영자가 현장에서 쓰기 쉬운 등수/작대/팀/공지 화면으로 개선한다.

핵심 방향은 다음과 같다.

- 시트 컬럼 위치 변화에 덜 취약한 파싱 구조로 변경
- 그룹별 등수, 동점 처리, 미기록 표시 등 대회 규칙 반영
- 운영자용 갱신/팀 배정 화면 추가
- 자동 갱신과 공유 시트 주소 설정으로 운영 편의성 개선
- 중복 스타일과 전역 캐시 구조를 정리해 유지보수성 확보

## 1단계: 데이터 모델과 파싱 안정화

### 작업

- `services/sheets.py`의 컬럼 인덱스 직접 접근을 헤더 기반 접근으로 교체한다.
- 참고 시트의 `rank` 탭은 헤더 행이 다음 형식이므로 이 값을 그대로 사용한다.
  - `작대`, `이름`, `순위`, `그룹`, `덤`, `합시`, `총합`
  - `1-1`, `1-2`, `1-3`, `1-4`, `1-5`, `1순 합`
  - `2-1`, `2-2`, `2-3`, `2-4`, `2-5`, `2순 합`
  - `3-1`, `3-2`, `3-3`, `3-4`, `3-5`, `3순 합`
- 헤더명을 코드 상수로 정의하고, 누락된 필수 컬럼은 명확한 오류로 표시한다.
- 각 참가자 행은 헤더와 값을 묶어 dict 형태로 만든 뒤 `row["총합"]`, `row["1-1"]`처럼 읽는다.
- 참가자 식별 기준을 정한다.
  - 기준: 이름
- `그룹` 값이 `-`인 참가자는 좌불로 분류한다.
  - 데이터에는 유지하되 순위 계산과 단체전 배정 대상에서는 제외한다.
  - 필요하면 운영자 화면에서 좌불 명단을 별도로 확인할 수 있게 한다.
- 점수 셀 파싱 규칙을 분리한다.
  - 한 순의 5개 시수 칸이 모두 빈 값이면 안 쏜 것으로 보고 `-`로 표시한다.
  - 한 순의 5개 시수 칸 중 `0`이 하나라도 적혀 있으면 쐈지만 0중으로 표시한다.
  - `1`은 명중으로 계산한다.
  - 잘못된 값: 파싱 오류 또는 0 처리 여부를 명시한다.
- `RankItem`, `NoticeItem`, `SquadItem` 타입을 실제 템플릿 사용 필드에 맞춰 정리한다.

### 완료 기준

- 헤더명이 유지되면 컬럼 순서가 바뀌어도 정상 동작한다.
- 한 순 전체 미시도 `-`와 0중 `0`이 화면에서 구분된다.
- 파싱 오류 원인이 서버 로그나 운영자 화면에서 확인 가능하다.

## 2단계: 등수 규칙 개선

### 작업

- `/rank`에 그룹별 필터 버튼을 추가한다.
  - 전체
  - 각궁
  - 그 외 구글 시트의 그룹 값
- `그룹` 값이 `-`인 좌불 참가자는 순위 목록에서 제외한다.
- 정렬 규칙을 명시적으로 함수화한다.
  - 총합 높은 순
  - 같은 합시면 전체 순위에서만 `각궁` 그룹 우선
  - 그다음 앞선 순 합시 우선
  - 그래도 같으면 먼저 맞춘 사람 우선
- 선다시수 계산은 기존 코드의 정렬 기준을 헤더 기반 필드로 옮긴다.
  - 순 합계 비교: `1순 합`, `2순 합`, `3순 합`
  - 먼저 맞춘 사람 비교: `1-1`, `1-2`, `1-3`, `1-4`, `1-5`, 필요하면 이후 순 시수까지 확장
- 그룹별 등수 보기에서도 순위 번호가 필터 결과 기준으로 표시되게 한다.

### 완료 기준

- `/rank?group=각궁` 같은 URL로 그룹 필터가 가능하다.
- 동점 상황에서 대회 규칙대로 순위가 재현된다.
- 정렬 로직은 템플릿이 아니라 Python 함수에서만 처리된다.

## 3단계: 작대 페이지 개선

### 작업

- `squad` 데이터에 참가자의 그룹 정보를 포함한다.
- `templates/squad.html`에서 작대 구성원 이름 옆에 그룹을 표시한다.
- 작대별 인원 수를 표시한다.
- 홈 버튼을 페이지 상단과 하단에 모두 배치한다.

### 완료 기준

- 작대 페이지에서 각 참가자의 그룹이 바로 보인다.
- 긴 작대 목록에서도 위/아래 어디서든 홈으로 이동할 수 있다.

## 4단계: 공통 UI와 화면 이동 정리

### 작업

- 모든 템플릿의 중복 CSS를 공통 레이아웃으로 분리한다.
  - 우선 Jinja base template 도입
  - 필요하면 `/static` 디렉터리 추가
- 홈 버튼을 주요 화면 상단/하단에 일관되게 배치한다.
- 모바일 화면에서 표가 깨지지 않도록 래퍼 또는 축약 표시를 추가한다.
- README의 실행 포트와 실제 포트 불일치를 정리한다.

### 완료 기준

- 버튼, 배경, 표 스타일이 화면마다 따로 복붙되지 않는다.
- `/rank`, `/board`, `/squad`, `/team`, `/api/refresh` 결과 화면의 이동 방식이 일관된다.

## 5단계: 팀 배정 알고리즘 개선

### 작업

- 현재 스네이크 드래프트 방식의 한계를 정리한다.
- `그룹` 값이 `-`인 좌불 참가자는 팀 배정 대상에서 제외한다.
- 더 균등한 팀 구성을 위해 탐색 기반 알고리즘을 추가한다.
  - 참가자 수가 적으면 조합 탐색 또는 백트래킹
  - 참가자 수가 많으면 그리디 + 국소 개선
- 팀별 합시 총합 차이를 지표로 계산한다.
- 운영자가 팀 수를 입력하고 결과를 미리 확인한 뒤 확정할 수 있게 한다.

### 완료 기준

- 같은 입력에 대해 기존 스네이크 방식보다 팀 총합 편차가 같거나 작다.
- 좌불 참가자가 단체전 팀에 포함되지 않는다.
- `/team` 화면에 팀별 합계와 최대/최소 차이가 표시된다.
- 팀 수가 비정상인 경우 사용자에게 명확히 안내한다.

## 6단계: 운영자 페이지 추가

### 작업

- `/admin` 페이지를 추가한다.
- 운영자 페이지에 다음 기능을 모은다.
  - 데이터 수동 갱신
  - 마지막 갱신 시간 확인
  - 팀 수 입력 후 팀 배정
  - 팀 배정 초기화
  - 현재 참가자 수, 공지 수, 작대 수 요약
- 기존 `/api/refresh`, `/api/team`, `/api/team/clear`는 폼 제출 또는 버튼에서 호출하도록 연결한다.

### 완료 기준

- 운영자는 URL을 직접 입력하지 않고 갱신과 팀 배정을 수행할 수 있다.
- 운영자 화면에서 현재 데이터 상태를 빠르게 확인할 수 있다.

## 7단계: 자동 갱신과 캐시 안정화

### 작업

- 서버 측 캐시 갱신에 `asyncio.Lock`을 적용해 동시 갱신 충돌을 막는다.
- 갱신 실패 시 기존 캐시를 유지하고 오류만 기록한다.
- 10초 자동 업데이트 방식을 결정한다.
  - 단순 구현: 브라우저에서 10초마다 새로고침
  - 개선 구현: JSON 상태 API 추가 후 필요한 영역만 갱신
- 공지 상황판 캐시 꼬임 재현 조건을 확인하고 테스트 케이스로 남긴다.

### 완료 기준

- 동시에 여러 번 갱신해도 캐시가 중간 상태로 노출되지 않는다.
- Google Sheets fetch 실패 시 기존 화면은 유지된다.
- `/board` 또는 전체 주요 화면이 10초 단위로 최신 데이터를 반영한다.

## 8단계: Google Sheets 주소 설정

### 작업

- 하드코딩된 `RANK_URL`, `NOTICE_URL`을 설정값으로 분리한다.
- 운영자 페이지에 구글 시트 공유주소 입력창을 추가한다.
- 입력받은 공유주소에서 publish CSV URL 또는 spreadsheet id/gid를 추출하는 방식을 정한다.
- 서버 재시작 후에도 설정을 유지할 저장 방식을 선택한다.
  - 간단한 방식: 로컬 JSON 설정 파일
  - 배포용 방식: 환경변수

### 완료 기준

- 코드 수정 없이 운영자가 시트 주소를 변경할 수 있다.
- 잘못된 주소를 입력하면 기존 설정을 덮어쓰지 않고 오류를 표시한다.

## 9단계: 중복 등록 검증

### 작업

- 구글 시트에서 중복 입력을 막는 방식을 우선 검토한다.
  - 조건부 서식
  - 데이터 유효성 검사
  - Apps Script
- 서버에서도 방어적으로 중복 참가자를 탐지한다.
- 중복 기준을 이름만으로 할지, 고유 ID 컬럼을 추가할지 결정한다.
- 운영자 페이지에 중복 의심 목록을 표시한다.

### 완료 기준

- 중복 참가자가 있으면 등수/작대/팀 배정 전에 운영자가 알 수 있다.
- 중복 검증 기준이 코드와 시트 운영 방식에 함께 문서화된다.

## 권장 구현 순서

1. 헤더 기반 파싱과 점수 표시 규칙 정리
2. 캐시 갱신 락과 실패 시 기존 캐시 유지
3. 그룹별 등수 필터와 동점 정렬 규칙 반영
4. 작대 페이지 그룹 표시와 홈 버튼 정리
5. 운영자 페이지 추가
6. 팀 배정 알고리즘 개선
7. 자동 갱신
8. 구글 시트 주소 설정
9. 중복 등록 검증

## 확정된 정책

- `각궁` 우선 규칙은 전체 순위에만 적용한다.
- 선다시수는 `합시 > 앞선 순 합시 > 먼저 맞춘 사람` 순서로 판단한다.
- `그룹` 값이 `-`인 참가자는 좌불로 보고 순위와 단체전 배정에서 제외한다.
- 참가자 중복 기준은 이름으로 판단한다.
- 구글 시트 주소 설정은 서버 재시작 후에도 유지한다.
- `rank` 탭 파싱은 `작대`, `이름`, `순위`, `그룹`, `덤`, `합시`, `총합`, `1-1` 등의 헤더명을 기준으로 한다.
